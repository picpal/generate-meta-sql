# 메타정보 관리 사용자 매뉴얼

> **대상**: 서비스 담당자 · 운영자 (메타를 직접 변경하는 사람)
> **이 문서**: 상황별로 "무엇을 어떤 순서로" 하는지만 다룬다.
> 명세는 [표준설계서](DB_메타정보_관리체계_표준설계.md), 설치 절차 전체는 [운영가이드](운영가이드.md).

---

## 0. 상황별 바로가기

| 지금 상황 | 이동 |
|---|---|
| 메타 체계를 처음 깔고 적재한다 | [§4](#4-처음-실행-최초-1회) |
| 적재는 끝났고 담당자·PCI·보관주기를 채워야 한다 | [§5](#5-초기-적재-직후-대량-보정) |
| 테이블을 새로 만들었다 / 만들 것이다 | [§6](#6-테이블-신규-생성) |
| 담당자·서비스·보관주기·`VIEW_YN`을 바꾼다 | [§7](#7-테이블-속성-변경) |
| 컬럼을 추가했다 | [§8](#8-컬럼-추가) |
| 컬럼 타입·속성·PCI 분류를 바꾼다 | [§9](#9-컬럼-수정) |
| 컬럼을 없앤다 | [§10](#10-컬럼-삭제) |
| 테이블을 없앤다 | [§11](#11-테이블-삭제) |
| **테이블을 지우고 같은 이름으로 다시 만들었다** | [§12](#12-테이블-재생성) |
| 인덱스 / 시퀀스를 손봤다 | [§13](#13-인덱스--시퀀스) |
| 비-PCI VIEW를 만들어 배포한다 | [§14](#14-비-pci-view-배포) |
| 실제 DB와 메타가 어긋난 것 같다 | [§15](#15-정기-점검) |
| 잘못 실행했다 | [§16](#16-잘못-실행했을-때) |
| 에러 코드가 떴다 | [§17](#17-자주-나는-오류) |

### 30초 용어

| 용어 | 뜻 |
|---|---|
| 본 테이블 / HIST | `TB_META_*` (현재 상태) / `TB_META_*_HIST` (변경 스냅샷) |
| `HIST_TYPE` | `I` 신규 · `U` 변경 · `D` 삭제 |
| SOFT / HARD 삭제 | 상태만 `DEPRECATED` / 물리·메타 실제 삭제 |
| `STATUS_CD` | `PLANNED` 계획 · `ACTIVE` 운영중 · `DEPRECATED` 폐기예정 |
| PCI | 개인신용정보(개인정보 포괄). `PCI_YN='Y'`면 비-PCI VIEW에서 제외 |
| `VIEW_YN` | 이 메타 행이 가리키는 실물이 뷰인지(`Y`) 테이블인지(`N`). §14 대상 판별 기준 |
| Drift | 실제 DB와 메타의 차이 |
| SSOT | 단일 진실 공급원 = 메타 테이블 |

---

## 1. 도구 두 가지 — 언제 무엇을 쓰나

| 도구 | 무엇 | 언제 |
|---|---|---|
| **`sql/` 스크립트** (기본) | 초기 적재 · 점검 · 변경 템플릿 · 대량 보정 | **거의 모든 경우.** 특히 대량 작업과 감사 대상 변경 |
| **`index.html` 웹앱** (보조) | 폼 입력 → DDL·메타 DML·HIST 묶음 생성 | 단건 변경을 SQL 손으로 쓰기 번거로울 때 |

웹앱은 **필수가 아니다.** 생성된 SQL은 어차피 사람이 검토한 뒤 실행하므로, 검토 대상이 정적 템플릿인 편이 반입 절차와 형식이 같다. 웹앱은 긴 `INSERT` 컬럼 목록을 손으로 안 써도 되는 편의 도구로 쓴다.

### 파일 지도

| 파일 | 용도 |
|---|---|
| [`sql/01_meta_ddl.sql`](sql/01_meta_ddl.sql) ~ [`03_initial_load.sql`](sql/03_initial_load.sql) | 최초 설치·적재 (§4) |
| [`sql/10_meta_change_templates.sql`](sql/10_meta_change_templates.sql) | **단건 변경 템플릿 9종** (§6~§13) |
| [`sql/11_bulk_backfill.sql`](sql/11_bulk_backfill.sql) | **대량 보정** (§5) |
| [`sql/04_drift_check.sql`](sql/04_drift_check.sql) · [`05_integrity_check.sql`](sql/05_integrity_check.sql) | 점검 (§15) |
| [`sql/06_func_idx_backfill.sql`](sql/06_func_idx_backfill.sql) | 함수기반 인덱스 표현식 보강 |
| [`sql/07_view_gen_nonpci.sql`](sql/07_view_gen_nonpci.sql) · [`07a`](sql/07a_view_review_sample.sql) | 비-PCI VIEW 배포 (§14) |
| [`sql/99_rollback.sql`](sql/99_rollback.sql) | 긴급 전체 폐기 |

---

## 2. 원칙 4가지

1. **메타 테이블이 유일한 원천(SSOT)** — 엑셀·워드로 관리하지 않는다.
2. **본 테이블 DML + `*_HIST` INSERT는 반드시 한 트랜잭션**, 마지막에만 `COMMIT;`.
3. **삭제는 SOFT 우선** (`STATUS_CD='DEPRECATED'`). HARD는 책임자 승인 후.
4. **DDL이 섞이면 롤백이 안 된다.** `ALTER`/`CREATE`/`DROP`은 implicit commit을 동반한다. 각 템플릿의 블록 순서를 바꾸지 않는다.

---

## 3. 공통 작업 흐름

### SQL 템플릿으로 (기본)

1. [`sql/10_meta_change_templates.sql`](sql/10_meta_change_templates.sql)에서 해당 § 블록을 **복사**한다 (파일 전체 실행 금지).
2. 상단 `DEFINE` 값을 채운다 — `EMP_ID`, `REASON`, `SCHEMA`, `TBL`, `COL` 등.
3. **대상 확인 SELECT**를 먼저 실행해 영향 행수를 본다. 예상과 다르면 중단.
4. `UPDATE`의 `SET` 절에서 **실제로 바꿀 줄만 남기고** 나머지 주석 줄을 지운다.
5. 본 DML → HIST INSERT → `COMMIT;` 순서로 실행.
6. 검증 SELECT 실행.

### 웹앱으로 (보조)

1. 좌측 하단 **담당자 사번** 입력 — 비우면 `HIST_BY`가 `'UNKNOWN'`으로 들어간다.
2. 상단 **변경 사유** 입력 (필수).
3. 탭(`Alt+1`~`Alt+4`) → 서브탭 선택 → 폼 입력.
4. `⌘↵` / `Ctrl+↵` 로 SQL 생성.
5. **출력을 검토하고 불필요한 블록을 지운 뒤** 실행.

### 두 경로 공통 규칙

| 항목 | 내용 |
|---|---|
| 변경 사유 | `NOT NULL`. **10자 이상 + 대상·사유·근거** 권장. 자동화 상수(`INITIAL_LOAD`, `SYSTEM_SYNC`)는 수동 경로에서 **사용 금지** |
| 식별자 | 영문 대문자·숫자·`_`만, **128자 이하**. 웹앱은 소문자를 대문자로 바꾸고 테이블명에 `TB_`가 없으면 자동으로 붙인다 |
| 작은따옴표 | 값 안의 `'`는 `''`(두 번 연속)으로 escape |
| 실행 계정 | 메타 테이블 소유 계정(또는 시노님 + DML 권한). 물리 DDL은 대상 스키마 DDL 권한도 필요 |

> ⚠️ **생성·복사한 SQL을 통째로 실행하지 않는다.** 이미 수행한 DDL 블록, `-- TODO:` 주석은 지우고 실행한다.

---

## 4. 처음 실행 (최초 1회)

### 4.1 사전 준비

- [ ] 메타 적재 대상 운영 스키마 목록 확정
- [ ] 메타 스키마 계정 + 운영 스키마 `ALL_*` 뷰 SELECT 권한 확보
- [ ] `'SVC1','SVC2'` 플레이스홀더를 사내 스키마명으로 **총 7곳** 치환
      — [`03_initial_load.sql`](sql/03_initial_load.sql) **5곳**(47·86·264·305·354행) + [`04_drift_check.sql`](sql/04_drift_check.sql) **2곳**(22·34행)

```bash
sed -i.bak "s/'SVC1','SVC2'/'OWNER1','OWNER2'/g" sql/03_initial_load.sql sql/04_drift_check.sql
grep -c "'SVC1','SVC2'" sql/03_initial_load.sql sql/04_drift_check.sql   # 주석 1건씩만 남아야 정상
```

### 4.2 실행 (단일 세션, 순서 고정)

| 순서 | 파일 | 필수 | 직후 검증 |
|:-:|---|:-:|---|
| 1 | [`01_meta_ddl.sql`](sql/01_meta_ddl.sql) | ✅ | 시퀀스 5 · 테이블 12 · **보조 인덱스 12** 생성 |
| 2 | [`02_common_code.sql`](sql/02_common_code.sql) | ✅ | `05` §5.1 코드 적재 |
| 2.5 | [`02a_cd_tos_template.sql`](sql/02a_cd_tos_template.sql) | 약관 사용 시 | `05` §5.1 |
| 3 | [`03_initial_load.sql`](sql/03_initial_load.sql) | ✅ | `05` **§5.2** + §5.3~§5.6 전부 통과 |

> **동시 실행 금지.** 두 세션이 같이 돌면 `ORA-00001`. 한 사람이 한 세션에서 끝까지 실행한다.
>
> **재실행 안전 범위에 주의.** `02`·`03`의 INSERT는 `NOT EXISTS` 가드가 있어 재실행해도 안전하다. **`01`의 DDL에는 가드가 없다** — 중간에 실패하면 어디까지 생성됐는지 확인하고 남은 객체만 만든다. `01` 전체를 그대로 재실행하면 `ORA-00955`가 난다.

### 4.3 적재 후 반드시 할 일

초기 적재는 **구조 메타만** 채운다. 아래는 기본값이므로 [§5](#5-초기-적재-직후-대량-보정)로 보정한다.

| 값 | 초기값 |
|---|---|
| `TB_META_TABLE.SERVICE_CD` | `UNASSIGNED` |
| `TB_META_TABLE.OWNER_EMP_ID` | `SYSTEM` |
| `PCI_YN` (테이블·컬럼) | `N` |
| `TB_META_COLUMN.SENSITIVITY_CD` | `LOW` |
| `TB_META_COLUMN.DEFAULT_VALUE` | `NULL` (LONG 타입이라 SQL-only 적재 불가) |
| `TB_META_INDEX_COLUMN.FUNC_EXPRESSION` | `NULL` (동일 사유) |

---

## 5. 초기 적재 직후 대량 보정

> 대상이 **수백~수천 행**이다. 웹앱에서 한 건씩 입력하는 방식은 쓰지 않는다.
> **실행 파일**: [`sql/11_bulk_backfill.sql`](sql/11_bulk_backfill.sql)

| § | 대상 | 방식 |
|---|---|---|
| §11.1 | `SERVICE_CD` (`UNASSIGNED` → 실제) | 스키마 단위 일괄 |
| §11.2 | `OWNER_EMP_ID` (`SYSTEM` → 사번) | 서비스 단위 일괄 |
| §11.3 | **PCI 분류** (`PCI_YN`·`PCI_CATEGORY_CD`·`SENSITIVITY_CD`·마스킹) | 컬럼명 정규식으로 **후보 추출** → 검토 → 분류별 일괄 |
| §11.4 | 보관주기 + 근거 | 서비스 단위 일괄 |
| §11.5 | `VIEW_YN` | `ALL_VIEWS`와 대조만 (0건이면 정상) |
| §11.6 | `DEFAULT_VALUE` | `ALL_TAB_COLUMNS.DATA_DEFAULT` 조회 후 수기 |

### 반드시 지킬 것

1. **§11.3의 정규식은 자동 분류가 아니다.** 후보를 좁히는 용도이며 오탐·누락이 모두 있다. 최종 판단은 담당자·보안팀이 한다.
2. **ID 고정 패턴을 쓴다.** `SERVICE_CD='UNASSIGNED'`인 행을 UPDATE하면 직후에는 그 조건이 성립하지 않아 HIST의 `WHERE`가 0건이 된다. 반대로 새 값으로 조회하면 이번 배치와 무관한 행까지 딸려온다. 그래서:
   - ① `LISTAGG`로 대상 `TABLE_ID` 목록을 뽑고
   - ② 눈으로 확인한 뒤 `DEFINE IDS`에 붙여넣고
   - ③ `UPDATE`와 `HIST INSERT` 모두 `WHERE TABLE_ID IN (&IDS)`로 같은 집합을 가리킨다
   - **한 배치는 200건 이하로.** `LISTAGG`는 4000 byte를 넘으면 잘리는 게 아니라 `ORA-01489`로 실패하고, `IN` 목록은 1,000개가 상한(`ORA-01795`)이다. 대상이 수천 건이면 `11` §11.0의 작업 테이블 방식을 쓴다.
   - 0건이면 `LISTAGG`가 `NULL`이 되어 이전 배치의 `IDS`가 남는다. 템플릿은 `NVL(...,'NULL')`로 감싸 `IN (NULL)`(미매칭)이 되게 해 두었다.
3. **`CHANGE_REASON`에 배치 식별자를 넣는다.** 예: `BACKFILL:SERVICE_CD:20260807`. 나중에 이 배치만 조회·소명할 수 있다.

### 완료 판정 (§11.7)

```sql
-- 모두 0이어야 보정 완료
SELECT (SELECT COUNT(*) FROM TB_META_TABLE  WHERE SERVICE_CD   = 'UNASSIGNED') AS SVC_UNASSIGNED,
       (SELECT COUNT(*) FROM TB_META_TABLE  WHERE OWNER_EMP_ID = 'SYSTEM')     AS OWNER_SYSTEM,
       (SELECT COUNT(*) FROM TB_META_COLUMN WHERE PCI_YN='Y' AND PCI_CATEGORY_CD IS NULL) AS PCI_NO_CATEGORY
  FROM DUAL;
```

---

## 6. 테이블 신규 생성

**템플릿**: `10` §10.4(컬럼) + §10.1(테이블 속성) · **웹앱**: 탭 01 → 신규 생성

| 상황 | 실행할 것 |
|---|---|
| 실제 테이블이 **아직 없다** | `CREATE TABLE` → 테이블 메타 INSERT → 컬럼 메타 INSERT → HIST(`I`) 2종 → `COMMIT` |
| 실제 테이블이 **이미 있다** | 위에서 `CREATE TABLE` 블록만 빼고 실행. 남기면 `ORA-00955` |
| 이미 있는 테이블이 **여러 개**다 | 하나씩 넣지 말고 [`03_initial_load.sql`](sql/03_initial_load.sql)을 **재실행**한다. 가드 덕에 누락분만 적재되고, 업무 메타는 이후 §5로 보정 |

### 웹앱 탭1 사용 시 주의

- **PK/UK는 `ADD CONSTRAINT` DDL이 생성되지만 FK는 생성되지 않는다.** FK 체크는 메타의 `FK_YN='Y'`만 남기고 DDL은 `-- TODO:` 주석으로 나온다. 참조 테이블·컬럼을 직접 써서 작성해야 하며, 작성하지 않을 거면 `FK_YN`도 `Y`로 두지 않는다.
- **'뷰 동시 생성' 옵션은 비-PCI VIEW가 아니다.** 전 컬럼을 노출하고 마스킹만 적용하며 이름은 `TB_MEMBER → VW_MEMBER`다. 보안 배포용은 §14를 쓴다.

### 검증

```sql
-- 05 §5.8.1 — 본 테이블과 최신 HIST가 같은 값인지
```

---

## 7. 테이블 속성 변경

> 담당자·서비스·보관주기·`VIEW_YN`·상태·격리 등 **업무·법적 메타** 갱신.
> **웹앱에는 이 기능이 없다** (탭1은 생성·삭제만). **템플릿**: `10` §10.1

1. 대상 확인 SELECT — 1건인지 본다
2. `UPDATE TB_META_TABLE` — 주석으로 열거된 선택 컬럼 중 **바꿀 줄만 남긴다**
3. `TB_META_TABLE_HIST` INSERT (`U`, 변경 후 스냅샷)
4. `COMMIT;`
5. 검증 (`05` §5.8.1)
6. 논리명을 바꿨다면 `COMMENT ON TABLE`도 실행 (DDL이므로 `COMMIT` 이후)

---

## 8. 컬럼 추가

**템플릿**: `10` §10.4 · **웹앱**: 탭 02 → 추가

1. **대상 테이블 메타가 있는지 먼저 확인**한다. 없으면 물리 DDL만 커밋되고 메타 INSERT가 실패한다
2. `ALTER TABLE ... ADD` (실제 컬럼을 이미 추가했다면 생략)
3. 메타 INSERT — `COLUMN_ORDER`를 비우면 `NVL(MAX(COLUMN_ORDER),0)+1`로 마지막에 붙는다
4. `TB_META_COLUMN_HIST` INSERT (`I`)
5. `COMMIT;`
6. 제약을 함께 추가한다면 그 다음에 `ADD CONSTRAINT` (DDL이므로 `COMMIT` 이후)

---

## 9. 컬럼 수정

**템플릿**: `10` §10.5 · **웹앱**: 탭 02 → 변경

### 9.1 먼저 현재값을 조회한다

`10` §10.5 (1)의 SELECT를 실행해 현재 정의를 확인한다. **바꾸지 않을 컬럼은 `SET` 절에서 지우면 그대로 유지된다.**

### 9.2 물리 변경이 있는지 구분한다

| 바꾸는 것 | `ALTER TABLE MODIFY` |
|---|---|
| 타입·길이·`DEFAULT`·`NOT NULL` | **실행** |
| PCI 분류·민감도·마스킹·보관주기·상태 등 메타만 | **실행하지 않음** |

### 9.3 웹앱을 쓸 때

**직접 수정한 항목만** `UPDATE`·`ALTER`에 포함된다. 손대지 않은 항목은 기존 메타값이 유지되므로 **바꿀 항목만 입력**하면 된다. 값을 비운 채 수정하면 `NULL`로 설정된다.

- 아무 항목도 수정하지 않으면 생성이 차단된다
- 타입·`DEFAULT`·`NULL`을 건드리지 않으면 `MODIFY` 블록이 아예 생성되지 않는다
- PK/UK/FK 블록도 해당 체크박스를 건드렸을 때만 나온다. 제약 **해제**는 `-- TODO:` 주석으로만 나오므로 제약명을 확인해 직접 작성한다

### 9.4 컬럼명 자체를 바꾸려면

`ALTER TABLE ... RENAME COLUMN`을 직접 작성한다. 메타는 UK(`TABLE_ID`, `COLUMN_NAME`)가 걸려 있으므로 **구 컬럼 SOFT 삭제 + 신 컬럼 추가**로 처리해 이력을 끊기지 않게 남긴다.

---

## 10. 컬럼 삭제

**템플릿**: `10` §10.6 · **웹앱**: 탭 02 → 삭제

| 방식 | 동작 |
|---|---|
| **SOFT** (권장) | 실제 컬럼 유지, 메타만 `STATUS_CD='DEPRECATED'` + HIST(`U`) |
| **HARD** | `ALTER TABLE DROP COLUMN` → 영향 인덱스 매핑 HIST(`D`) → 인덱스 HIST(`D`) → 메타 DELETE → 컬럼 HIST(`D`) → 컬럼 DELETE → `COMMIT` → 카탈로그 기준 인덱스 재적재 |

> ⚠️ **물리 DROP을 먼저 한다.** 메타를 먼저 지우고 커밋한 뒤 물리 DROP이 실패하면, 실물은 남았는데 그것을 정리할 근거가 사라진다. 반대 방향은 `04` §8.2가 잡아내므로 복구할 수 있다.
> 이 컬럼을 포함한 인덱스는 Oracle이 정리하므로, 영향 인덱스 메타를 걷어낸 뒤 `ALL_IND_COLUMNS` 기준으로 재적재한다.

---

## 11. 테이블 삭제

**템플릿**: `10` §10.2(SOFT) / §10.3(HARD) · **웹앱**: 탭 01 → 삭제

| 방식 | 동작 |
|---|---|
| **SOFT** (권장) | `TB_META_TABLE.STATUS_CD='DEPRECATED'` + HIST(`U`). 자식 메타는 그대로 — 필요하면 컬럼·인덱스도 각각 SOFT 처리 |
| **HARD** | ① 게이트 확인 ② `DROP TABLE` ③ INDEX_COLUMN HIST→DELETE ④ INDEX HIST→DELETE ⑤ COLUMN HIST→DELETE ⑥ TABLE HIST→DELETE ⑦ `COMMIT` |

### HARD 삭제 전 확인 2가지

1. **외부 참조 FK** — 이 테이블을 참조하는 FK가 있으면 `DROP TABLE`이 `ORA-02449`로 실패한다. `10` §10.3 (0)의 SELECT로 먼저 본다.
2. **연결 시퀀스 메타** — `TB_META_SEQUENCE`는 `TABLE_ID` FK가 없어서 **자동으로 정리되지 않는다.** `10` §10.3 (4-3)의 SELECT로 확인한다. 방치한 채 §12에서 같은 시퀀스를 다시 등록하면 `UK_META_SEQUENCE` 위반이 난다.

---

## 12. 테이블 재생성

> 시나리오: **실제 테이블을 DROP하고 같은 이름으로 다시 만들었다. 메타를 지우고 새로 넣고 싶다.**
> **템플릿**: `10` §10.9

### 12.1 전제

- `TB_META_TABLE`에 **UNIQUE (`SCHEMA_NAME`, `TABLE_NAME`)** 가 있다.
- 따라서 **SOFT 삭제로 남겨둔 채 같은 이름을 다시 INSERT할 수 없다** (`ORA-00001`).
- 즉 이 시나리오는 **메타 HARD 정리가 전제**다. 승인을 받는다.
- 이력은 끊기지 않는다 — HARD 정리 시 `D`, 재적재 시 `I`가 남는다.
- 단 **`TABLE_ID`는 새 값**이 된다. 예전 `TABLE_ID`를 참조하던 리포트를 함께 고친다.

### 12.2 순서 (실제 테이블을 이미 재생성한 경우)

| # | 작업 |
|:-:|---|
| 1 | 백업 — `10` §10.9 (1) |
| 2 | 옛 메타 정리 — `10` §10.3의 **(2-1) ~ (5-2)** 실행 후 `COMMIT` |
| 3 | ★ **(1) 물리 DROP은 실행하지 않는다** — 새로 만든 테이블이 삭제된다 |
| 4 | ★ **(0-1)(0-2) 게이트** 결과를 확인한다 (외부 FK · 연결 시퀀스) |
| 5 | 정리 확인 — `10` §10.9 (3), **0건**이어야 한다 |
| 6 | 새 메타 적재 — 아래 A 또는 B |
| 7 | 인덱스 재등록 — `10` §10.7 등록의 (2)~(5). **물리 DDL 블록은 실행하지 않는다** |
| 8 | 시퀀스 — 4단계에서 살아 있었다면 **재등록하지 않는다** |
| 9 | 검증 — `05` §5.8 + §5.3~§5.6 + `04` §8.1~§8.3 모두 통과 |

**6단계 두 방법**

| | 내용 |
|---|---|
| **A. 자동 재적재** (권장) | [`03_initial_load.sql`](sql/03_initial_load.sql) 재실행. 구조 메타만 채워지고 `CHANGE_REASON='INITIAL_LOAD'`로 들어간다. 업무 메타는 §7·§9로 보정 |
| **B. 수기 적재** | `10` §10.4를 컬럼 수만큼 반복 + §10.1로 테이블 속성 지정. 재생성 사유를 `CHANGE_REASON`에 남길 수 있어 감사 추적이 명확 |

### 12.3 변형

| 상황 | 차이 |
|---|---|
| 실제 테이블을 **아직 안 지웠다** | 2단계에서 (1) 물리 DROP을 **남긴다**. 이후 `CREATE TABLE` 하고 6단계에서 DDL 블록 제외 |
| 컬럼 몇 개만 바뀐 재생성 | 전체 재적재보다 §8·§9·§10 조합이 안전하다. `TABLE_ID`가 보존된다 |

### 12.4 하지 말 것

- ❌ SOFT 삭제 후 같은 이름으로 새 INSERT (UK 위반)
- ❌ `*_HIST` 행 직접 DELETE — 감사 증적이다. 잘못 들어간 이력도 지우지 않고 정정 이력을 새로 남긴다
- ❌ 3단계에서 `DROP TABLE` 블록을 남긴 채 실행

---

## 13. 인덱스 / 시퀀스

### 인덱스 — `10` §10.7 · 웹앱 탭 03

| 작업 | 순서 |
|---|---|
| 등록 | `CREATE INDEX` → `TB_META_INDEX` INSERT → `TB_META_INDEX_COLUMN` INSERT → HIST(`I`) **2종** |
| 삭제 | 대상 확인 → `DROP INDEX` → 매핑 HIST(`D`) → 인덱스 HIST(`D`) → 매핑 DELETE → 메타 DELETE → `COMMIT` |

> **삭제 시 대상 테이블명은 필수다.** 인덱스명만으로 지우면 다른 스키마의 동명 인덱스 메타까지 사라진다. 실행 전 확인 SELECT가 **정확히 1건**인지 본다.
> 함수기반 인덱스의 `FUNC_EXPRESSION`은 자동으로 안 채워진다 → [`06_func_idx_backfill.sql`](sql/06_func_idx_backfill.sql).

### 시퀀스 — `10` §10.8 · 웹앱 탭 04

| 작업 | 주의 |
|---|---|
| 등록 | `CREATE SEQUENCE` → 메타 INSERT → HIST(`I`) |
| 변경 | **바꿀 옵션만** `ALTER`·`SET`에 들어간다. 변경 전 메타와 `ALL_SEQUENCES` 양쪽을 조회해 현재값을 확인한다 (`10` §10.8 (2)) |
| 삭제 | `DROP SEQUENCE` → HIST(`D`) → 메타 DELETE → `COMMIT` |

> `CREATE_DDL` 컬럼은 '생성 DDL 원문'이므로 `ALTER`로 갱신되지 않는다. 원문을 추적한다면 별도로 UPDATE 한다.

---

## 14. 비-PCI VIEW 배포

**순서를 절대 바꾸지 않는다.** 1단계가 끝나기 전에 3단계로 가면 PCI 오분류가 그대로 노출된다.

| # | 행위자 | 작업 |
|:-:|---|---|
| 1 | 데이터/보안 담당자 | `VIEW_YN`(§7)·`PCI_YN`(§9 또는 §5) 검토·확정 |
| 2 | 보안팀 | [`07a_view_review_sample.sql`](sql/07a_view_review_sample.sql) SPOOL → 비-PCI 컬럼 실데이터로 오분류 육안 검증. 발견 시 **1단계 복귀** |
| 3 | 운영자 | [`07_view_gen_nonpci.sql`](sql/07_view_gen_nonpci.sql) SPOOL → DDL + 점검(2)(3) 검토. 점검에 행이 잡히면 1단계 복귀 |
| 4 | 운영자 | SPOOL된 DDL을 단일 세션에서 일괄 실행 |

### 반드시 알아둘 것

- **출력은 3개 문장이다** — `CREATE VIEW` + `GRANT SELECT ... TO <SCHEMA>_V` + `CREATE SYNONYM <SCHEMA>_VIEW....`. 세 개를 모두 검토하고, 대상 롤·시노님 스키마가 존재하는지 먼저 확인한다.
- **이름 규칙은 `VW_<원본 TABLE_NAME 전체>`** 다. 원본이 `TB_MEMBER`면 `VW_TB_MEMBER`. 웹앱 탭1의 '뷰 동시 생성'(`VW_MEMBER`)과는 **다른 물건**이며 그쪽은 PCI 필터가 없다.
- 재배포 시 동일명 VIEW가 있으면 `ORA-00955` → `DROP VIEW` 선행.
- **자동 실행 금지** (SPOOL → 검토 → 수동 실행).

---

## 15. 정기 점검

| 주기 | 실행 | 기대 | 결과가 나오면 |
|---|---|---|---|
| 일 1회 | `04` §8.1 | 0건 | 실DB엔 있고 메타에 없음 → `03` 재실행 또는 §6 |
| 일 1회 | `04` §8.2 | 0건 | 메타는 ACTIVE인데 실DB에 없음 → §11 SOFT 삭제 검토 |
| 일 1회 | `04` §8.3 | 0건 | 컬럼 정의 불일치 → 정답 확정 후 §9로 동기화 |
| 변경 직후 | `05` **§5.8.1~5.8.3** | 값 일치 | 본↔HIST 불일치 → HIST INSERT 누락 확인 |
| 주 1회 | `05` **§5.8.4** | 전부 0건 | HIST 없는 고아 행 → HIST 없이 본 테이블만 손댄 경로가 있다 |
| 주 1회 | `05` **§5.8.5** | 0건 | `HIST_BY='UNKNOWN'` 또는 자동화 상수 오용 → 정정 이력 추가 적재 |
| 변경 직후 + 주 1회 | `05` §5.3~§5.6 | 0건 | 코드값 무결성 위반 |

> ⚠️ **`05` §5.2는 초기 적재(03) 직후 전용이다.** hist 카운트가 `CHANGE_REASON='INITIAL_LOAD'`인 `I` 이력만 세기 때문에, 초기 적재 이후 무언가를 하나라도 추가하면 영구히 불일치로 보인다. **정상이며 결함이 아니다.** 일상 검증은 §5.8을 쓴다.

---

## 16. 잘못 실행했을 때

| 상황 | 조치 |
|---|---|
| `COMMIT;` 전이고 **DDL을 실행하지 않았다** | `ROLLBACK;` — 끝 |
| DDL이 한 번이라도 실행됐다 | implicit commit이 이미 발생했다. **롤백 불가.** 아래 3가지를 확인해 수동 복구 |
| ↳ 확인 1 | `TB_META_*_HIST` 5종에 이력이 적재됐는가 |
| ↳ 확인 2 | `TB_META_INDEX_COLUMN` / `INDEX` / `COLUMN` / `TABLE` 메타가 어디까지 지워졌는가 |
| ↳ 확인 3 | Oracle 실제 객체가 남아 있는가 (`ALL_TABLES`, `ALL_INDEXES`, `ALL_SEQUENCES`) |
| `COMMIT` 후, DML만 실행됨 | 반대 방향 UPDATE/DELETE를 **HIST 동반**으로 새로 수행. 사유에 `"오적재 정정: ..."` 명시 |
| 메타 전체를 갈아엎어야 함 | [`99_rollback.sql`](sql/99_rollback.sql) — ⚠️ 메타 스키마 전체 폐기. **책임자 승인 필수** |

> 위험한 작업(HARD 삭제·재생성) 전에는 백업을 먼저 뜬다 — `10` §10.9 (1).

---

## 17. 자주 나는 오류

| 에러 | 원인 | 조치 |
|---|---|---|
| `ORA-00001` unique constraint | ① 두 세션 동시 실행 ② SOFT 삭제 상태에서 같은 이름 재INSERT ③ 시퀀스 메타 중복 등록 | ① 한 세션만 남기고 재실행 ② §12 경로로 ③ §11의 (4-3) 점검 |
| `ORA-00955` name already used | 이미 있는 테이블/인덱스/뷰/시퀀스의 DDL을 안 지우고 실행. `01` 전체 재실행 | 해당 DDL 블록 삭제 |
| `ORA-02292` child record found | HARD 삭제에서 자식→부모 순서를 어김 | 템플릿 블록 순서대로 재실행 |
| `ORA-02449` unique/primary keys referenced by foreign keys | 외부 FK가 참조 중인 테이블을 DROP | `10` §10.3 (0)으로 참조 FK 확인 후 정리 |
| `ORA-02260` / `02261` / `02264` | 이미 있는 PK/UK/제약명을 다시 추가 | 제약 블록 삭제 또는 제약명 변경 |
| `ORA-01430` column being added already exists | 이미 있는 컬럼을 다시 ADD | `ALTER ... ADD` 블록 삭제 |
| `ORA-01400` cannot insert NULL | 대상 메타가 없어 서브쿼리가 `NULL` 반환 (예: 테이블 메타 없이 컬럼 추가) | 상위 메타를 먼저 등록 |
| `ORA-12899` value too large | 설명·비고·변경사유가 컬럼 byte 한도 초과 (`DESCRIPTION` 2000, `REMARK` 4000, `CHANGE_REASON` 2000) | 값을 줄인다 |
| `ORA-00054` resource busy | 다른 세션 락 | `05` §5.7로 세션 식별 후 정리 |
| `ORA-00904` invalid identifier (`FN_MASK_*`) | 마스킹 함수가 사내에 미정의 | 함수 정의 확인 또는 마스킹 미적용으로 생성 |
| 화면: "변경 사유 필수" / "변경할 항목을 하나 이상 수정하세요" | 상단 사유 미입력 / 폼을 하나도 안 건드림 | 입력 후 재생성 |
| HIST에 `HIST_BY='UNKNOWN'` | 사번 미입력 상태로 생성한 SQL을 실행 | 사번 입력 → 정정 이력 추가 적재 (`05` §5.8.5로 탐지) |

---

## 18. 금지 사항

- ❌ 본 테이블만 바꾸고 `*_HIST` INSERT 생략
- ❌ `*_HIST` 행 UPDATE / DELETE (감사 증적)
- ❌ 담당자 수동 경로에서 `CHANGE_REASON`에 `INITIAL_LOAD`·`SYSTEM_SYNC` 사용
- ❌ 트리거·프로시저·함수·익명 PL/SQL 블록 작성 (폐쇄망·금융권 정책)
- ❌ 메타 정의를 엑셀·워드로 별도 관리
- ❌ 2개 이상 세션에서 메타 SQL 동시 실행
- ❌ 템플릿 파일 전체 실행 / 생성된 SQL 검토 없이 붙여넣기 실행
- ❌ 대량 보정에서 후보 추출 결과를 확인하지 않고 UPDATE

---

## 참고 문서

- [운영가이드.md](운영가이드.md) — 설치 절차·운영 주의사항·트러블슈팅 전체
- [DB_메타정보_관리체계_표준설계.md](DB_메타정보_관리체계_표준설계.md) — 표준 명세·설계 의도
- [README.md](README.md) — 앱 실행·디렉토리 구조
- [SQL_검증리포트.md](SQL_검증리포트.md) — 반입 전 SQL 검증 결과
