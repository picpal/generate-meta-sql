# 사내 운영 가이드

> **대상**: DB 메타정보 관리체계 (Oracle 19c)
> **선행 검증**: `SQL_검증리포트.md` (권고 #1~#3 반영 완료)
> **본 문서 범위**: SQL 실행 절차 · 운영 주의 4건 · 정합성 점검 · 트러블슈팅
> **모든 실행 SQL은 [`sql/`](sql/) 디렉토리에 분리되어 있음.** 본 문서는 절차와 포인터만 기재한다.

---

## ⚡ 0. 초기 실행 순서 (필수)

> **반드시 아래 순서대로 단일 세션에서 직렬 실행한다.** 순서를 어기면 FK 무결성 위반 또는 코드 미존재로 ROLLBACK 발생.

| 순서 | 실행 SQL | 필수/선택 | 목적 |
|:---:|---|:---:|---|
| **1** | [`sql/01_meta_ddl.sql`](sql/01_meta_ddl.sql) | **필수** | 시퀀스 5 + 테이블 12 + 보조 인덱스 12 생성 |
| **2** | [`sql/02_common_code.sql`](sql/02_common_code.sql) | **필수** | 공통코드 10종 + TB_META_CODE_HIST 적재 |
| **2.5** | [`sql/02a_cd_tos_template.sql`](sql/02a_cd_tos_template.sql) | 선택 | 사내 약관 코드(CD_TOS) 적재 — 약관 사용 시 필수 |
| **3** | [`sql/03_initial_load.sql`](sql/03_initial_load.sql) | **필수** | 시스템 카탈로그 → 메타 테이블 + HIST 적재 |

> **사전 작업**: 3번 실행 전 [`sql/03_initial_load.sql`](sql/03_initial_load.sql)과 `04_drift_check.sql` 맨 위의 `DEFINE TARGET_SCHEMAS`(각 1줄)를 사내 운영 스키마명으로 지정 — 본 문서 §3 참조.

### 운영 점검·보강 (수시 / 필요 시)

| 분류 | 실행 SQL | 시점 |
|---|---|---|
| 정합성 검증 | [`sql/05_integrity_check.sql`](sql/05_integrity_check.sql) | 02·03 직후 + 일상 점검 |
| Drift 감지 | [`sql/04_drift_check.sql`](sql/04_drift_check.sql) | 일 1회 배치 권장 |
| 함수기반 인덱스 보강 | [`sql/06_func_idx_backfill.sql`](sql/06_func_idx_backfill.sql) | SYS_NCxxxxx 발견 시 |
| 비-PCI VIEW 검토 시트 | [`sql/07a_view_review_sample.sql`](sql/07a_view_review_sample.sql) | **07 실행 전** — 보안팀이 비-PCI 컬럼 실데이터로 PCI 오분류 검증 — §9.A 참조 |
| 비-PCI VIEW DDL 생성 | [`sql/07_view_gen_nonpci.sql`](sql/07_view_gen_nonpci.sql) | **선행**: 담당자가 `PCI_YN`/`VIEW_YN` 검토 완료 후 — §9.A 참조 |
| 긴급 전체 롤백 | [`sql/99_rollback.sql`](sql/99_rollback.sql) | ⚠️ 신중 — 책임자 승인 |

---

## 1. 패키지 구성

```
generate-meta-sql/
├── README.md                              앱 사용 안내
├── DB_메타정보_관리체계_표준설계.md       표준 설계서 (명세 · 표 · 설계의도)
├── SQL_검증리포트.md                      반입 전 검증 리포트
├── 운영가이드.md                          본 문서
├── 사용자매뉴얼.md                        담당자용 상황별 매뉴얼 ★
├── sql/                                   ★ 모든 실행 SQL (단일 진실 공급원)
│   ├── 01_meta_ddl.sql                    [순서 1, 필수]   DDL 일괄
│   ├── 02_common_code.sql                 [순서 2, 필수]   공통코드 적재
│   ├── 02a_cd_tos_template.sql            [순서 2.5, 선택] 사내 약관 코드
│   ├── 03_initial_load.sql                [순서 3, 필수]   카탈로그 → 메타 적재
│   ├── 04_drift_check.sql                 [수시] 메타 ↔ 실제 DB Drift 감지
│   ├── 05_integrity_check.sql             [수시] 코드/HIST 정합성 점검
│   ├── 06_func_idx_backfill.sql           [선택] 함수기반 인덱스 사후 보강
│   ├── 07_view_gen_nonpci.sql             [선택] 비-PCI VIEW DDL 자동 생성
│   ├── 07a_view_review_sample.sql         [선택] 비-PCI VIEW 생성 전 보안검토 실데이터 시트
│   ├── 10_meta_change_templates.sql       [운영] 메타 증분 변경 템플릿 9종 ★
│   ├── 11_bulk_backfill.sql               [운영] 초기 적재 직후 대량 보정 ★
│   └── 99_rollback.sql                    [긴급] 메타 스키마 전체 폐기
├── index.html · css/ · js/                폐쇄망 단독 SQL 생성기 웹앱
```

---

## 2. 운영 주의 #1 — CD_TOS 코드그룹 사내 적재

### 배경
표준설계서 §5는 `CD_TOS`(연계 이용약관)를 *"사내 이용약관 체계에 맞게 적재"*로 위임. `02_common_code.sql`은 이 그룹을 의도적으로 누락. `03_initial_load.sql`은 TB_META_TABLE/COLUMN의 `TOS_CD`에 NULL을 적재하므로 03 실행 자체에는 영향 없음.

### 절차
실행 위치는 **02 직후 ~ 03 직전** (사내 약관 사용 시 필수).

> **실행 SQL**: [`sql/02a_cd_tos_template.sql`](sql/02a_cd_tos_template.sql) — 약관 코드 INSERT + TB_META_CODE_HIST 동시 적재 패턴이 이미 작성되어 있음. 사내 실제 약관 코드로 교체 후 실행.
>
> **검증**: 실행 후 본↔HIST 행수 일치 — [`sql/05_integrity_check.sql`](sql/05_integrity_check.sql) §5.1 (코드 적재 검증) 참조.

---

## 3. 운영 주의 #2 — 대상 스키마 지정

### 배경
[`sql/03_initial_load.sql`](sql/03_initial_load.sql)과 [`sql/04_drift_check.sql`](sql/04_drift_check.sql)은 어느 스키마를 메타 대상으로 삼을지 모른다. 각 파일 맨 위 **`DEFINE TARGET_SCHEMAS` 한 줄씩, 총 2곳**을 사내 운영 스키마명으로 고친다.

```sql
DEFINE TARGET_SCHEMAS = "'OWNER1','OWNER2','OWNER3'"
```

`03`은 이 값 하나를 5개 WHERE 절(테이블·뷰·인덱스·인덱스컬럼·시퀀스)에서, `04`는 2곳(§8.1의 `ALL_TABLES`·`ALL_VIEWS`)에서 참조한다. **본문 SQL은 손대지 않는다.**

> 바깥 큰따옴표는 SQL\*Plus용이다. 값 자체는 작은따옴표로 감싼 목록이어야 한다.
> 스키마가 1개면 `"'OWNER1'"` 도 OK.

### 반드시 지킬 것

1. **두 파일의 값을 같게 유지한다.** 다르면 `04`의 drift 점검이 `03`이 적재하지 않은 스키마를 "메타 누락"으로 계속 보고한다.
2. **`SET VERIFY ON`이라 실행 시 치환 결과가 화면에 찍힌다.** 첫 실행에서 실제 스키마 목록이 의도한 것인지 눈으로 확인한다.
3. 값을 비운 채 실행하면 `IN ()`이 되어 **문법 오류로 즉시 중단된다.** 조용히 0건 적재되는 일은 없다.

### 지정 검증
```bash
grep -n "DEFINE TARGET_SCHEMAS" sql/03_initial_load.sql sql/04_drift_check.sql
# 2건이 동일 값인지 육안 확인
```

---

## 4. 운영 주의 #3 — 동시 실행 금지 / 단일 세션 직렬

### 배경
모든 INSERT는 NOT EXISTS 가드로 재실행 안전. 그러나 **2개 세션이 동시 실행**하면 둘 다 가드 통과 → 두 번째 INSERT는 PK/UK 제약 위반(ORA-00001) → 트랜잭션 ROLLBACK.

### 운영 규칙
1. **단일 세션 직렬 실행**: 01 → 02 → (02a) → 03 (§0 표 순서)
2. **각 SQL 파일 끝의 `COMMIT;`이 반영 보장** — 다음 단계 시작 전 자동 처리
3. **부분 실패 후 재실행**: `02`·`03`의 INSERT는 NOT EXISTS 가드로 안전 — 누락분만 보충됨.
   ⚠️ **`01`의 DDL에는 가드가 없다.** 중간 실패 시 생성된 객체를 확인하고 남은 것만 만든다. 전체 재실행하면 `ORA-00955`.

### 실행 전 Lock 점검
> **실행 SQL**: [`sql/05_integrity_check.sql`](sql/05_integrity_check.sql) §5.7 (동시 실행 / Lock 충돌 진단)

다른 세션이 메타 테이블에 락을 걸고 있으면 그 세션을 KILL 하고 진행.

### Lock 충돌 시 대응
| 에러 | 원인 | 대응 |
|---|---|---|
| ORA-00054 (resource busy) | 다른 세션 lock | `v$session` 확인 → 세션 정리 |
| ORA-00001 (unique constraint) | 동시 실행 의심 | 한 세션만 남기고 종료 → 재실행 |

---

## 5. 운영 주의 #4 — 함수기반 인덱스 SYS_NCxxxxx 사후 보강

### 배경
함수기반 인덱스(예: `CREATE INDEX IDX_X ON T(UPPER(NAME))`)는 시스템 가상 컬럼 `SYS_NC00001$`을 자동 생성. `ALL_IND_COLUMNS.COLUMN_NAME`이 이 값으로 노출됨. 표현식은 `ALL_IND_EXPRESSIONS.COLUMN_EXPRESSION`(LONG)에 저장되지만 SQL-only로는 추출 불가 → 03이 `FUNC_EXPRESSION`을 NULL로 둠.

### 절차

> **실행 SQL**: [`sql/06_func_idx_backfill.sql`](sql/06_func_idx_backfill.sql)
>
> 구성 (SQL-only, PL/SQL 블록 없음):
> - §6.1 보강 대상 + `ALL_IND_EXPRESSIONS.COLUMN_EXPRESSION`(LONG) 화면 표시 SELECT
> - §6.2 수기 UPDATE 템플릿 (주석) — §6.1 결과 각 행마다 작성·실행
> - §6.3 HIST 적재 (`HIST_TYPE='U'`, `CHANGE_REASON='FUNC_EXPRESSION_BACKFILL'`)
> - §6.4 보강 결과 검증 SELECT
>
> **실행 순서**:
> 1. `SET LONG 32767` / `SET LINESIZE 32767` 선행 (LONG 화면 표시 폭 확보)
> 2. `@06_func_idx_backfill.sql` 1차 실행 — §6.1 결과 화면 출력
> 3. §6.1 출력 결과 보고 §6.2 템플릿대로 `UPDATE TB_META_INDEX_COLUMN SET FUNC_EXPRESSION = '…' WHERE INDEX_ID=? AND COLUMN_POS=?;` 수기 작성·실행
> 4. `@06_func_idx_backfill.sql` 2차 실행 — §6.3 HIST 자동 적재 + §6.4 검증
>
> **권한 요건**: 보강 대상 스키마에 대한 SELECT 권한 + 메타 스키마 UPDATE 권한.
>
> **주의**:
> - `COLUMN_EXPRESSION` 길이가 2000자 초과 시 앞 2000자만 입력 (`FUNC_EXPRESSION VARCHAR2(2000)`)
> - 값 내부 작은따옴표는 `''`(두 번 연속)으로 escape
>
> **대안**: 표준설계서 §7.3 단서 "DB 밖 도구가 별도 보강" — 외부 ETL(Python `oracledb`/`cx_Oracle` 등)에서 LONG 자동 변환 가능. 사내 도구 표준이 있다면 그쪽을 우선.

---

## 6. 사내 반입 후 실행 체크리스트

### 사전
- [ ] 본 패키지 사내 반입 (zip 해제)
- [ ] 메타 적재 대상 운영 스키마 목록 확정 (예: APP1, APP2, COMMON)
- [ ] CD_TOS 사내 코드 체계 확보 ([`sql/02a_cd_tos_template.sql`](sql/02a_cd_tos_template.sql) 편집)
- [ ] 메타 스키마 계정 확보
- [ ] 메타 스키마 → 운영 스키마 SELECT 권한 확보:
      `ALL_TABLES`, `ALL_TAB_COLUMNS`, `ALL_INDEXES`, `ALL_IND_COLUMNS`, `ALL_IND_EXPRESSIONS`, `ALL_SEQUENCES`, `ALL_TAB_COMMENTS`, `ALL_COL_COMMENTS`, `ALL_CONSTRAINTS`, `ALL_CONS_COLUMNS`, `ALL_ENCRYPTED_COLUMNS`, `ALL_VIEWS`

### 치환 (§3)
- [ ] [`sql/03_initial_load.sql`](sql/03_initial_load.sql) `DEFINE TARGET_SCHEMAS` 지정
- [ ] [`sql/04_drift_check.sql`](sql/04_drift_check.sql) `DEFINE TARGET_SCHEMAS` **동일 값**으로 지정
- [ ] `grep -n "DEFINE TARGET_SCHEMAS" sql/03_initial_load.sql sql/04_drift_check.sql` 로 두 값이 같은지 확인

### 실행 (단일 세션 직렬 — §0 순서 필수)
- [ ] **순서 1**: [`sql/01_meta_ddl.sql`](sql/01_meta_ddl.sql) 실행 → 시퀀스 5 + 테이블 12 + 보조 인덱스 12 생성
- [ ] **순서 2**: [`sql/02_common_code.sql`](sql/02_common_code.sql) 실행
- [ ] **순서 2.5** (선택): [`sql/02a_cd_tos_template.sql`](sql/02a_cd_tos_template.sql) 편집 후 실행
- [ ] **검증**: [`sql/05_integrity_check.sql`](sql/05_integrity_check.sql) §5.1 (코드 적재) 통과
- [ ] **순서 3**: [`sql/03_initial_load.sql`](sql/03_initial_load.sql) 실행
- [ ] **검증**: [`sql/05_integrity_check.sql`](sql/05_integrity_check.sql) §5.2 (본↔HIST 1:1), §5.3~§5.6 (코드값 무결성) 통과

### 사후 (수기 매핑 / 보강)
- [ ] `SERVICE_CD='UNASSIGNED'` 행을 사내 서비스 코드로 매핑 (HIST 'U' 동반)
- [ ] `OWNER_EMP_ID='SYSTEM'` 행을 실 담당자로 매핑
- [ ] PII/PCI 분류, SENSITIVITY 등급, 보관주기 재분류
- [ ] 함수기반 인덱스 식별 → [`sql/06_func_idx_backfill.sql`](sql/06_func_idx_backfill.sql) 실행 (필요 시)
- [ ] (선택) `index.html` 사내 폐쇄망 웹서버 배포 → 증분 변경 관리 시작

---

## 7. 정합성 검증 쿼리

> **실행 SQL**: [`sql/05_integrity_check.sql`](sql/05_integrity_check.sql)

| 쿼리 | 시점 | 기대 결과 |
|---|---|---|
| §5.1 코드 적재 검증 | 02 직후 | 코드그룹별 행수 일치 (CD_RETENTION_PERIOD 6, CD_PCI_CATEGORY 5, CD_SENSITIVITY 3, CD_ISOLATION_LEVEL 3, CD_STATUS 3, CD_INDEX_TYPE 5, CD_INDEX_PURPOSE 6, CD_SEQUENCE_PURPOSE 4, CD_MASKING_RULE 7, CD_SERVICE 1) |
| §5.2 본 ↔ HIST 행수 | 03 직후 | 모든 행에서 `main = hist` |
| §5.3 코드값 무결성 (테이블) | 03 직후 + 수시 | 0건 |
| §5.4 SENSITIVITY_CD 검증 | 03 직후 | 0건 (모두 'LOW' 적재 + 'LOW'가 코드 마스터에 존재) |
| §5.5 컬럼 코드값 무결성 | 03 직후 + 수시 | 0건 |
| §5.6 인덱스/시퀀스 코드값 무결성 | 03 직후 + 수시 | 0건 |
| §5.7 Lock 충돌 진단 | 트러블슈팅 | 메타 테이블에 락 건 세션 식별 |

### 카탈로그 ↔ 메타 Drift 감지 (일 1회 배치 권장)

> **실행 SQL**: [`sql/04_drift_check.sql`](sql/04_drift_check.sql)

| 쿼리 | 의미 | 대응 |
|---|---|---|
| §8.1 메타 누락 | 실제 DB에 있는데 메타에 없는 테이블 | 03 재실행으로 자동 보충 |
| §8.2 실제 누락 | 메타 ACTIVE인데 실제 DB에 없는 객체 | STATUS_CD를 'DEPRECATED'로 변경 검토 |
| §8.3 컬럼 정의 불일치 | 양방향 — 타입/길이/정밀/스케일/NULL | 메타 또는 실제 중 어느 쪽이 맞는지 확정 후 동기화 |

---

## 8. 트러블슈팅

### Q1. ORA-12899 (value too large for column "DESCRIPTION")
- **원인**: ALL_TAB_COMMENTS/ALL_COL_COMMENTS 코멘트가 2000자 초과
- **상태**: 권고 #2 반영(SUBSTR 절단)으로 발생 안 함
- **그래도 발생 시**: 권고 #2가 누락된 구버전 사용 의심 → 최신 [`sql/03_initial_load.sql`](sql/03_initial_load.sql) 사용 확인

### Q2. ORA-00001 (unique constraint violated)
- **원인**: 동시 세션 실행으로 NOT EXISTS 가드를 둘 다 통과
- **대응**: 한 세션 KILL → ROLLBACK 된 세션 재실행 (가드가 새로 적재된 행은 스킵)

### Q3. ORA-00942 (table or view does not exist) — ALL_ENCRYPTED_COLUMNS 등
- **원인**: TDE 옵션 미설치 환경
- **대응**: Oracle 19c는 SE 라이선스에서도 빈 뷰로 존재하므로 보통 발생 안 함. 발생 시 DBA에게 뷰 권한 확인.

### Q4. 03 §7.3.2 인덱스 컬럼 적재 후 일부 INDEX_ID에 컬럼 행이 없음
- **원인**: TB_META_INDEX 헤더 적재(§7.3.1)는 성공했으나 ALL_IND_COLUMNS에 해당 행이 없는 경우
- **대응**:
  ```sql
  SELECT mi.INDEX_NAME FROM TB_META_INDEX mi
   WHERE NOT EXISTS (SELECT 1 FROM TB_META_INDEX_COLUMN ic WHERE ic.INDEX_ID = mi.INDEX_ID);
  ```
  결과 인덱스를 ALL_INDEXES, ALL_IND_COLUMNS에서 직접 조회하여 원인 확인.

### Q5. SVC 스키마 누락 발견
- **원인**: 사내 반입 시점에 누락된 운영 스키마
- **대응**:
  1. [`sql/03_initial_load.sql`](sql/03_initial_load.sql) 4개 IN 절에 누락 스키마 추가 (또는 [`sql/04_drift_check.sql`](sql/04_drift_check.sql) §8.1도)
  2. 03 재실행 → NOT EXISTS 가드로 기존 행 보존, 신규 스키마만 적재
  3. [`sql/05_integrity_check.sql`](sql/05_integrity_check.sql) §5.2로 본↔HIST 정합성 재검증

### Q6. 시퀀스 적재 시 SEQ_META_*가 자기 자신으로 적재됨
- **상태**: 권고 #3 반영(`SEQ_META_%` 자기 제외)으로 발생 안 함
- **그래도 발생 시**: 권고 #3 누락 구버전 사용 의심 → 최신 SQL 사용 또는 사후 정리:
  ```sql
  DELETE FROM TB_META_SEQUENCE_HIST WHERE SEQUENCE_ID IN (
      SELECT SEQUENCE_ID FROM TB_META_SEQUENCE WHERE SEQUENCE_NAME LIKE 'SEQ_META_%'
  );
  DELETE FROM TB_META_SEQUENCE WHERE SEQUENCE_NAME LIKE 'SEQ_META_%';
  COMMIT;
  ```

---

## 9. 증분 변경 관리 (반입 후 일상 운영)

초기 적재 이후 메타 변경은 **[`sql/10_meta_change_templates.sql`](sql/10_meta_change_templates.sql)의 템플릿을 기본 경로**로 사용한다. 대량 보정(SERVICE_CD·담당자·PCI 분류 등)은 [`sql/11_bulk_backfill.sql`](sql/11_bulk_backfill.sql)을 쓴다. [`index.html`](index.html) 웹앱은 단건 변경 SQL을 폼으로 생성하는 **보조 도구**이며 필수가 아니다.

변경 직후 검증은 [`sql/05_integrity_check.sql`](sql/05_integrity_check.sql) **§5.8**(일상 변경 전용)을 쓴다. §5.2는 초기 적재 직후 전용이므로 증분 변경 후에는 영구 불일치로 표시된다. Drift 감지([`sql/04_drift_check.sql`](sql/04_drift_check.sql))는 일 1회 배치 권장.

> 담당자용 상황별 절차는 [사용자매뉴얼.md](사용자매뉴얼.md) 참조.

| 변경 유형 | 웹앱 탭 | HIST_TYPE |
|---|---|---|
| 테이블 신규/속성 변경/폐기 | 탭1 테이블 | I/U/D |
| 컬럼 추가/변경/삭제 | 탭2 컬럼 | I/U/D |
| 인덱스 생성/삭제 | 탭3 인덱스 | I/D |
| 시퀀스 생성/변경/삭제 | 탭4 시퀀스 | I/U/D |

모든 운영 변경은 **본 테이블 INSERT/UPDATE/DELETE + `*_HIST` 스냅샷 INSERT**를 동일 트랜잭션에서 실행. 웹앱이 자동 생성하는 SQL 묶음을 그대로 실행.

### 9.A 비-PCI VIEW 일괄 생성 워크플로

**반드시 아래 순서**를 따른다. 메타 검토가 끝나기 전에 VIEW 일괄 생성을 실행하면 PCI 누락·오분류가 그대로 노출 VIEW에 반영된다.

| 단계 | 행위자 | 작업 |
|:---:|---|---|
| **1** | 데이터/보안 담당자 | 메타 검토 — `TB_META_TABLE.VIEW_YN`, `TB_META_COLUMN.PCI_YN` 지정 (웹앱 탭1/탭2로 SQL 생성 → 본 + HIST 동일 트랜잭션 실행) |
| **2** | 보안팀 | VIEW 생성 전 [`sql/07a_view_review_sample.sql`](sql/07a_view_review_sample.sql) SPOOL 실행 → 비-PCI 컬럼의 실데이터 1건으로 PCI 오분류(Y여야 하는데 N) 육안 검증. 오분류 발견 시 1단계로 복귀 |
| **3** | 운영자 | 검토 완료 후 [`sql/07_view_gen_nonpci.sql`](sql/07_view_gen_nonpci.sql) SPOOL 실행 — (1) 본 쿼리 / (2) 비-PCI 0개 점검 / (3) 식별자 128byte 초과 점검 |
| **4** | 운영자 | SPOOL된 `CREATE VIEW VW_<원본>` DDL을 단일 세션에서 일괄 실행 (**갱신 재배포 시** 동일명 VIEW 존재 → `ORA-00955`, `DROP VIEW <SCHEMA>.VW_<원본>;` 선행 필요. 최초 생성은 정상) |

> **점검 (2) (3)** 결과에 행이 있으면 1단계로 복귀해 메타 보정 후 재실행 권장. 자동 실행 금지(SPOOL → 검토 → 수동 실행).

> 상세 규칙·예외는 표준설계서 §9 참조.

---

## 10. 백업 · 롤백

### 사전 백업 (선택, 정책에 따라)
```sql
-- 02 실행 후 (코드 마스터 백업)
CREATE TABLE BAK_TB_META_CODE_<YYYYMMDD> AS SELECT * FROM TB_META_CODE;

-- 03 실행 후 (전체 스냅샷)
CREATE TABLE BAK_TB_META_TABLE_<YYYYMMDD>    AS SELECT * FROM TB_META_TABLE;
CREATE TABLE BAK_TB_META_COLUMN_<YYYYMMDD>   AS SELECT * FROM TB_META_COLUMN;
CREATE TABLE BAK_TB_META_INDEX_<YYYYMMDD>    AS SELECT * FROM TB_META_INDEX;
CREATE TABLE BAK_TB_META_SEQUENCE_<YYYYMMDD> AS SELECT * FROM TB_META_SEQUENCE;
```

### 전체 롤백 (긴급)
> **실행 SQL**: [`sql/99_rollback.sql`](sql/99_rollback.sql) — ⚠️ 메타 스키마 전체 폐기. 책임자 승인 후 실행.

### 부분 재실행
모든 INSERT는 NOT EXISTS 가드 → 안전하게 재실행 가능. 별도 사전 정리 불필요.

---

## 11. 참고 문서

- [사용자매뉴얼.md](사용자매뉴얼.md) — 담당자용 상황별 매뉴얼 (컬럼 수정 · 테이블 생성/삭제/재생성 등)
- [README.md](README.md) — 웹앱 사용법
- [DB_메타정보_관리체계_표준설계.md](DB_메타정보_관리체계_표준설계.md) — 표준 설계서 (명세 · 설계 의도)
- [SQL_검증리포트.md](SQL_검증리포트.md) — 반입 전 검증 + 권고 반영 현황
