# 변경 이력

**반입 패키지 단위**의 이력이다. 사내에 어떤 버전이 들어가 있는지,
그 버전에서 무엇이 달라졌는지를 여기서 확인한다.

현재 버전은 루트 [`VERSION`](VERSION) 파일에 있고, 앱 화면 좌측 상단과
패키지 안 `BUILD_INFO.txt` 에서도 확인할 수 있다.

버전 표기는 `MAJOR.MINOR.PATCH` 를 따른다.

| 자리 | 올리는 경우 |
|---|---|
| MAJOR | 메타 테이블 구조 변경 등 **재적재·마이그레이션이 필요한** 변경 |
| MINOR | 기능·SQL 템플릿 추가, 하위 호환 유지 |
| PATCH | 결함 수정, 문서 정정 |

> 이 파일은 1.4.0 부터 작성됐다. 그 이전 이력은 git 커밋 기록을 따른다.

---

## [1.5.0] — 2026-08-07

테이블 재생성 절차를 **클린 → `03` 재실행 → 복원** 세 단계로 단순화했다.

### 변경

- `sql/10` **§10.9 테이블 재생성**을 자립형 절차로 재작성했다.
  - 기존에는 "§10.3의 (2-1)~(5-2)만 실행하고 (1) 물리 DROP은 실행하지 마시오"였다.
    골라내기에 실패하면 **방금 만든 테이블이 삭제된다.**
    이제 §10.9에는 물리 DDL이 한 줄도 없어 통째로 실행해도 실물이 안전하다.
  - **인덱스 재등록 단계를 없앴다.** `03`은 INDEX·INDEX_COLUMN·SEQUENCE까지
    카탈로그에서 적재하고 모든 가드가 행 단위 `NOT EXISTS`라, 지운 테이블만
    정확히 다시 채워진다. §10.7을 따로 돌릴 이유가 없었다.
  - 수기 적재(옛 방법 B)를 걷어냈다. 자동 재적재 + 복원이 항상 낫다.
- `사용자매뉴얼.md` §12를 위 절차에 맞춰 재작성했다.

### 추가

- **업무 메타 스냅샷·복원** (`10` §10.9 (1)·(4)).
  재적재된 행은 `PCI_YN='N'`, `SENSITIVITY_CD='LOW'`,
  `SERVICE_CD='UNASSIGNED'` 로 돌아온다. 이 상태에서 비-PCI VIEW를 재생성하면
  **이전에 가려지던 개인신용정보 컬럼이 노출된다.**
  클린 전에 업무 메타를 떠 두고 재적재 후 되돌린다.
  - 타입·길이·PK/UK/FK·암호화는 **복원하지 않는다** — 새로 만든 테이블의 실제
    정의가 정답이고, 옛 값을 덮어쓰면 `04` §8.3이 잡는 불일치를 스스로 만든다.
  - 컬럼은 이름으로 매칭하고 `MATCHED`/`NEW`/`REMOVED` 를 복원 전에 보여준다.
    `NEW` 는 PCI 분류를 새로 해야 하는 컬럼이다.

### 수정

- 매뉴얼 §11·§17이 시퀀스 점검 위치를 `10` §10.3 **(4-3)** 으로 잘못 가리키고
  있었다. 실제 위치는 **(0-2)** 다.

---

## [1.4.0] — 2026-08-07

버저닝·패키징을 스크립트화했다. 어떤 패키지가 어떤 소스인지 특정 가능해졌다.

### 추가

- `VERSION` — 패키지 버전의 단일 진실 공급원
- `build/package.sh` — 반입 패키지 빌드. 테스트 통과·버전 일치·작업 트리 청결을
  모두 만족해야 zip이 나온다. 패키지 안에 커밋 해시와 빌드 시각(`BUILD_INFO.txt`)을 남긴다.
- `build/bump-version.sh` — 버전이 박힌 세 곳(`VERSION`, `js/version.js`,
  `index.html` 의 `?v=`)을 한 번에 갱신. 버전 하향은 거부한다.
- `js/version.js` — 화면 좌측 상단에 버전 표시.
  압축을 푼 뒤에는 파일명이 사라지므로 폐쇄망에서 유일한 식별 수단이 된다.
- `CHANGELOG.md` — 이 파일

### 수정

- **정적 리소스 캐시 무효화 누락.** `index.html` 의 `?v=N` 이 손으로 관리되고 있었고,
  1.3.11 에서 `column.js` 를 대폭 수정했는데도 `?v=1` 에 머물러 있었다.
  이미 앱을 쓰던 사용자의 브라우저는 **수정 전 JS를 계속 실행했다** —
  폼 기본값이 메타를 덮어쓰는 결함이 고쳐지지 않은 상태로.
  이제 모든 `?v=` 가 앱 버전을 따르고, 불일치 시 `package.sh` 가 빌드를 막는다.

### 참고

앱 버전(`1.3.11`)과 GitHub 릴리즈 태그(`v1.0.0`)가 서로 다른 계보로 굴러가고 있었다.
높은 쪽인 앱 버전을 기준으로 통일했다 — 화면에 이미 노출되던 번호라
낮추면 사용자가 구버전을 받았다고 오해한다.

---

## [1.3.11] — 2026-08-07 · GitHub 릴리즈 [`v1.0.0`](https://github.com/picpal/generate-meta-sql/releases/tag/v1.0.0)

사내 폐쇄망 반입용 첫 정식 패키지. Oracle 19c 기준.
릴리즈 태그는 `v1.0.0` 이지만 이 소스의 앱 버전은 `1.3.11` 이었다.

### 추가

- `sql/10_meta_change_templates.sql` — 단건 변경 템플릿 9종
  (테이블 속성/신규/삭제/재생성, 컬럼 추가·수정·삭제, 인덱스, 시퀀스)
- `sql/11_bulk_backfill.sql` — 초기 적재 직후 대량 보정
  (담당자·PCI 분류·보관주기). 폼으로는 불가능한 수백~수천 행 단위 작업.
- `sql/05_integrity_check.sql` §5.8 — 변경 직후 검증 7종
  (본↔최신 HIST 비교, 배치 건수, 고아 행, `HIST_BY` 오용, 전 컬럼 MINUS, HARD 삭제 검증)
- `사용자매뉴얼.md` — 담당자용 상황별 실행 순서
- `tests/` — JS 단언 116개 + SQL INSERT 정합성 검사 90개

### 변경

- **메타 변경의 기본 경로가 SQL 템플릿으로 전환됐다.**
  웹앱은 단건 변경 SQL을 폼으로 생성하는 보조 도구이며 필수가 아니다.
- 삭제 순서를 **물리 `DROP` 먼저, 메타 삭제 나중**으로 뒤집었다.
  실패 시 "물리는 없고 메타는 남은" 상태가 되는데 `04 §8.2`로 탐지·복구가 가능하다.
  반대 순서는 정리에 필요한 정보 자체가 사라진다.

### 수정

폼 기본값이 운영 메타를 조용히 덮어쓰던 문제를 구조적으로 제거했다.

- 체크박스 3종(PCI/암호화/마스킹)의 DOM id가 규칙에서 벗어나 **입력값이 아예 읽히지 않던** 결함
- 변경 폼에 `(변경 안 함)` 빈 항목 추가 — 손대지 않은 항목은 `SET`에 나오지 않는다
- 시퀀스 `ALTER`가 미변경 절까지 내보내던 문제
  (`NOMINVALUE`/`NOCACHE` 명시로 물리·메타 의미 일치)
- 인덱스 삭제 시 대상 테이블명 필수화 + `TABLE_ID` 범위 한정
- `TB_META_INDEX_COLUMN` HIST 누락 보완
- 물리 변경이 실제로 있을 때만 `MODIFY` DDL 생성

### 알려진 한계

1. **실 Oracle 미실행.** `sql/10`·`11`은 정적 분석만 거쳤다.
   개발 DB에서 §10.1 한 건을 먼저 시연할 것.
2. **브라우저 미검증.** 테스트는 최소 DOM 스텁 기반이라 실제 렌더링·이벤트 바인딩을 거치지 않는다.
3. `sql/10` §10.6의 `LISTAGG → IN` 은 대상이 매우 많으면 길이 한계에 걸린다.
   `11`의 작업 테이블 방식으로 우회한다.
